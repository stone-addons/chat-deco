{
  "private": true,
  "devDependencies": {
    "minecraft-scripting-types-server": "1.0.0-alpha.1",
    "stoneserver-scripting-types": "latest",
    "typescript": "latest",
    "typescript-bundle": "latest"
  },
  "scripts": {
    "build": "tsc-bundle tsconfig.json --entryPoint main",
    "watch": "tsc-bundle tsconfig.json --watch --entryPoint main"
  }
}
